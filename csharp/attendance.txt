using System;
using Suresh;

namespace Attendance.Tracking
{
    class Attendance1
    {
        static void Main()
        {
            string title = "The Attendance for Today";

            Console.WriteLine("Current Date: " + MyDate.GetCurrentDate());
            Console.WriteLine("Current Time: " + MyDate.GetCurrentTime());
            Console.WriteLine("Current Year: " + MyDate.GetCurrentYear());

            string[] listofstudents = { "Anu", "Shravya" };
            Console.WriteLine(title);
            foreach (string element in listofstudents)
            {
                Console.Write(element);
            }
        }
    }
}
